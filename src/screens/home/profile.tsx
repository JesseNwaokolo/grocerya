import SafeArea from "@/components/SafeArea";
import { View, Text } from "react-native";

const Profile = () => {
  return (
    <SafeArea>
      <View>
        <Text>Profile</Text>
      </View>
    </SafeArea>
  );
};

export default Profile;
